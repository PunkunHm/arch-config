# Host configuration for PURIN-X13
# Personal Laptop
# Bootstrapped from BlackDon's configuration

host: PURIN-X13
description: Personal Laptop

# Import shared configurations (optional)
# Example:
# import:
#   - hosts/shared/laptop-common.yaml

# Enabled modules
enabled_modules: []

# Host-specific packages
packages: []

# Exclude packages from base or modules
exclude: []

# Configuration backup settings
config_backups:
  enabled: true # Auto-backup on sync
  max_backups: 5 # Keep last 5 backups (0 = unlimited)

# Settings
flatpak_scope: user
auto_prune: false
backup_tool: timeshift

# AUR helper (auto-detects paru or yay if not specified)
aur_helper: yay # Options: paru, yay, or any compatible AUR helper
